---
import PasteForm from "../../components/solidjs/PasteForm";
import Default from "../../layouts/Default.astro";
import { Image } from "@astrojs/image/components";

const url = Astro.request.url;

let title = "Personal Pastebin";
let description = "Yeah, I'm a programmer. What do you expect?";
---

<Default title={title} description={description}>
  <div class="relative min-h-[18rem] overflow-hidden w-full border-b border-neutral-700 flex items-center">
    <Image
      src="/profile/unsplash-bg.webp"
      alt="Unsplash Background by Alex Perez"
      class="min-h-[18rem] max-h-full w-full object-cover brightness-50 absolute inset-0"
      height={720}
      width={1560}
      loading="eager"
    />
    <header class="relative px-4 text-center lg:text-left text-neutral-200 gap-2 max-w-3xl w-full mx-auto flex flex-col justify-center">
      <h1 class="font-heading font-bold text-4xl">
        {title}
      </h1>
      <p class="text-xl text-neutral-400">
        {description}
      </p>
    </header>
  </div>
  <section class="w-full py-12 z-10 relative">
    <div class="max-w-3xl mx-auto px-4 prose prose-invert prose-p:text-neutral-400 prose-headings:text-neutral-200 prose-headings:font-bold prose-headings:font-heading prose-headings:pb-4 prose-headings:border-b prose-headings:border-neutral-700">
      <PasteForm url={url} client:load />
      <h2>
        Rationale
      </h2>
      <p>
        So I've been using <a href="https://pastebin.com/" class="text-neutral-300 hover:text-neutral-200">Pastebin</a> for a while now.
        Not a shabby service, but I've always wanted to make my own. So yeah, here it is.
      </p>
      <h2>
        Terms of Usage
      </h2>
      <ul>
        <li>
          You can use this service to store any text you want as long as it doesn't violate any known laws, doesn't promote hate speech,
          and certainly doesn't contain any illegal or highly sensitive information.
        </li>
        <li>
          This service only stores your pastes for a maximum of 7 days. After that, they will be deleted.
        </li>
        <li>
          This service is provided as-is, without any warranty. I am not responsible for any damages caused by this service.
        </li>
        <li>
          This service is not affiliated with Pastebin in any way.
        </li>
      </ul>
    </div>
  </section>
</Default>